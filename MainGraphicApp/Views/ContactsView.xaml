<UserControl x:Class="MainGraphicApp.Views.ContactsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainGraphicApp.Views"
             xmlns:viewModels="clr-namespace:MainGraphicApp.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ContactViewModel}"
             
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{Binding Title}" />
        </StackPanel>
        
        <Button Grid.Row="1" Padding="8" Margin="5 10" Content="Add New Contact" Command="{Binding GoToAddContactCommand}" />

        <Button Grid.Row="2" Padding="8" Margin="5 10" Content="Edit A Contact" Command="{Binding GoToEditContactCommand}" />

    </Grid>
</UserControl>
